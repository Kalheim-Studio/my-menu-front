import { Link } from "react-router-dom";

const ResetPassword = () => {
    return (
        <div>
            <p>Reset Password</p>
            <Link to="/">Retour</Link>
        </div>
    );
};

export { ResetPassword };
